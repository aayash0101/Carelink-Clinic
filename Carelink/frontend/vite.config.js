import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    host: '127.0.0.1',
    port: 5173,
    strictPort: true,
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:5000',
        changeOrigin: true,
        secure: false,
        // ✅ leave this out unless backend sets Domain=localhost explicitly
        // cookieDomainRewrite: '127.0.0.1',
      },
    },
  },
})
